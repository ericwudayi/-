//載入MySQL模組
var mysql = require('mysql');
//建立連線
var connection = mysql.createConnection({
    host: '127.0.0.1',
    user: 'mql_AT_leotek',
    password: 'a!XuvBaaT8JNW@AV',
    database: 'leotek'
});

//開始連接
connection.connect();

//接著就可以開始進行查詢
connection.query('SELECT 1 + 1 AS solution',function(error, rows, fields){
    //檢查是否有錯誤
    if(error){
        throw error;
    }
    console.log(rows[0].solution); //2
});